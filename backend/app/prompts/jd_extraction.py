"""JD解析提示词"""

JD_EXTRACTION_PROMPT = """
你是招聘专家，请从以下职位描述（JD）中提取结构化需求：

【JD文本】
{jd_text}

【提取要求】
1. 职位名称（title）
2. 领域/部门（domain）
3. 必须技能列表（skills_required，最多10项）
4. 加分技能列表（skills_nice，最多10项）
5. 最低学历要求（min_education：专科/本科/硕士/博士，若未提及填None）
6. 最低工作年限（min_work_years：整数年，若未提及填0）
7. 工作地点（locations：城市列表，若未提及留空）
8. 薪资范围（salary：如“20-30K”或“面议”）
9. JD摘要（description：不超过50字）

【输出格式】
严格返回JSON，不要添加任何解释。
"""


# 阶段1：归一化要点整理（仅输出要点，便于第二阶段结构化）
JD_NORMALIZE_PROMPT = """
将以下 JD 文本压缩为要点清单，按以下顺序输出简洁要点：
1) 职位名称
2) 领域/部门
3) 必须技能（转换为编程语言或者类似于docker啊dify啊pytorch工具等等之类的，自行提取转换）
4) 加分技能（转换为编程语言或者类似于docker啊dify啊等等工具之类的，自行提取转换）
5) 最低学历（专科/本科/硕士/博士，未知则留空）
6) 最低工作年限（整数，未知填0）
7) 工作地点（城市列表，未知则留空）
8) 薪资范围（如20-30K或面议，未知则留空）
9) JD摘要（不超过50字）

要求：只输出纯文本要点，每行一条，禁止解释性语句与JSON。

【JD文本】
{jd_text}
"""


